__pycache__/
*.pyc

# live camera/depth snapshots (debug visuals, not source)
.snapshots/

# machine-local Claude Code settings (absolute paths, per-session ids)
.claude/settings.local.json

# JetBrains IDE project metadata
.idea/
